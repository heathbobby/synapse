# Synapse Orchestration Knowledge

This directory is commit-able project knowledge for the artifact factory. It is
separate from `.orchestration/runtime/`, which contains generated task cards,
memos, status reports, and iteration outputs that must stay out of git.

Use this directory for stable lessons learned, workflow evaluation summaries,
and reusable notes that should influence future artifact-generation loops.
